<div class="writing-dashboard">
  <div class="header-section">
    <h1>Writing Dashboard</h1>
    <p class="section-description">
      This is your central hub for all writing-related activities. Collaborate with others, create stories, and develop your creative works.
    </p>
  </div>
  
  <div class="writing-features">
    <div class="feature-card">
      <div class="feature-icon">
        <i class="ph-duotone ph-chat-centered-text"></i>
      </div>
      <h3>Threads</h3>
      <p>Join or create collaborative writing threads with other writers</p>
      <a href="/writing/threads" class="btn btn-primary">Go to Threads</a>
    </div>
    
    <!-- Other writing features can be added here in the future -->
  </div>
</div>

<style>
  .writing-dashboard {
    max-width: 1000px;
    margin: 0 auto;
  }
  
  .header-section {
    text-align: center;
    margin-bottom: var(--spacing-xl);
  }
  
  .section-description {
    color: var(--muted-text);
    max-width: 700px;
    margin: 0 auto;
  }
  
  .writing-features {
    display: flex;
    gap: var(--spacing-lg);
    justify-content: center;
    flex-wrap: wrap;
  }
  
  .feature-card {
    background-color: var(--card-bg);
    border-radius: var(--radius-md);
    padding: var(--spacing-lg);
    width: 300px;
    display: flex;
    flex-direction: column;
    align-items: center;
    text-align: center;
    box-shadow: var(--shadow-sm);
    transition: transform var(--transition-fast), box-shadow var(--transition-fast);
  }
  
  .feature-card:hover {
    transform: translateY(-5px);
    box-shadow: var(--shadow-md);
  }
  
  .feature-icon {
    font-size: 48px;
    color: var(--header);
    margin-bottom: var(--spacing-md);
  }
  
  .feature-card h3 {
    margin-bottom: var(--spacing-sm);
  }
  
  .feature-card p {
    color: var(--muted-text);
    margin-bottom: var(--spacing-lg);
  }
  
  @media (max-width: 768px) {
    .feature-card {
      width: 100%;
    }
  }
</style>
