<div class="hero-section">
  <div class="hero-content">
    <div class="hero-title">northern attitude</div>
    <p class="hero-subtitle">forgive my northern attitude - i was raised on little light</p>
    
    <% if (user) { %>
      <div class="hero-buttons">
        <a href="/dashboard" class="btn btn-lg btn-primary">Go to Dashboard</a>
      </div>
    <% } else { %>
      <div class="hero-buttons">
        <a href="/auth/register" class="btn btn-lg">Get Started</a>
        <a href="/auth/login" class="btn btn-lg">Login</a>
      </div>
    <% } %>
  </div>
</div>